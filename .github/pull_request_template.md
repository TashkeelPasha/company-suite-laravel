## What changed

<!-- Two or three sentences. What did you change and why? -->

## Related issues / tickets

<!-- Fixes #123, Refs #456 — or "none" -->

## How to test

<!-- Steps a reviewer can follow locally. If your change is UI, add a screenshot. -->

1.
2.
3.

## Checklist

- [ ] CI is green (all checks pass)
- [ ] I ran `php artisan route:list` locally with no errors
- [ ] If I added/changed API endpoints, I updated `API.md`
- [ ] If I added/changed a DB column, I added a migration (never edited an existing one)
- [ ] If I added a new .env variable, I added it to `.env.example`
- [ ] I did **not** commit `.env`, `vendor/`, `node_modules/`, or `database/database.sqlite`

## Deployment notes

<!-- Any manual step needed after merge (e.g. "run php artisan storage:link on prod"). -->
